services:
  web:
    build: .
    container_name: colbywest-dev
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      # Persist TTT stats if they are written to a local file in /app/data
      - ./data:/app/data
